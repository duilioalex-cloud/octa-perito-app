# OCTA Perito v0.6.9

## Hotfix visual do painel financeiro e operacional

Corrige a publicação do dashboard sem os estilos específicos, que causava textos concatenados, grids ausentes e gráficos sem formatação.

Não há alteração no banco de dados nem nova migração SQL.

Antes do upload ao GitHub, confirme que o pacote não contém `package-lock.json`, `.npmrc`, `node_modules` nem `.next`.
